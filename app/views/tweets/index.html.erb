<h1>index.html</h1>
<%= form_with(model: @tweet, url: tweets_path, local: true) do |f| %>
  <%= f.text_field :content %>
  <%= f.submit '送信' %>
<% end %>
<% @tweets.each do |tweet| %>
  <div class="content">
    <%= tweet.content %>
    <%= tweet.created_at %>
  </div>
<% end %>